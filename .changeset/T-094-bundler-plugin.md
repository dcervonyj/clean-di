---
"clean-di-codegen": minor
---

Add Vite and Rollup bundler plugins. Import from `clean-di-codegen/vite` or `clean-di-codegen/rollup` and codegen runs automatically as part of your build — no separate CLI step.
